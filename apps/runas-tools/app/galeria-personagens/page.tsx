import { PageContainer } from "@/components/layout/page-container"
import { CharacterGallery } from "@/components/gallery/character-gallery"

export default function CharacterGalleryPage() {
  return <PageContainer title="Galeria de Personagens" description="Salve, compare, exporte e alterne entre até 100 fichas, organizadas em 5 páginas."><CharacterGallery /></PageContainer>
}
